/// Custom mem cache class
final class CustomMemCache {
  /// Creates a [CustomMemCache] instance.
  const CustomMemCache({required this.height, required this.width});

  /// The height of the image.
  final int height;

  /// The width of the image.
  final int width;
}
